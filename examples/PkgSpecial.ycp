/**
 * PkgSpecial example - not for general use!
 **/
{
    UI::OpenDialog(
	       `VBox(
		     `Label( "Hello, world!" ),
		     `PkgSpecial( "dummy1" ),
		     `PkgSpecial( "dummy2" ),
		     `PkgSpecial( "dummy3" ),
		     `PushButton( `opt(`default), "&Ok" )
		     )
	       );
    UI::UserInput();
    UI::CloseDialog();
}
