#
# Makefile.cvs
#

PREFIX = /usr

configure: all
	./configure --prefix=$(PREFIX)

all:
	y2tool y2autoconf
	y2tool y2automake
	autoreconf --force --install -v

install: configure
	make
	make install

reconf: all
	./config.status --recheck
	./config.status
